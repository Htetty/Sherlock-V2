# Local development Redis for the investigation queue.
#   npm run redis:up
#   npm run redis:down
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
